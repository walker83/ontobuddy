# 构建产物
/bin/
/dist/
/myonto
*.exe
*.test

# Go 工作文件
*.out
coverage.txt

# IDE / 编辑器
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# 用户级本体运行时数据（按需取消注释）
# ontology.ttl
# .myonto.toml

# 可视化输出（生成的力导向图 HTML，不进 git）
/*.html
# internal/ 下的 HTML 是 go:embed 模板，必须追踪
skills/myonto/bin/

# 对拍生成的报告（make crosscheck 产出，属运行时产物）
eval/report.md

